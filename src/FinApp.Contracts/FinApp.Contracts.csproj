<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net9.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <!-- No FinApp.Domain reference: Contracts is pure wire DTOs. The one file that pulled Domain
       (AccountSnapshotSerializer) moved into FinApp.Domain (Path B, docs/DOMAIN-REMOVAL.md). -->

</Project>
